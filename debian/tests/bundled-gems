#!/bin/sh

set -e

rc=0
while read gem version repository; do
  if ruby2.5 -e "gem '${gem}', '>= ${version}'"; then
    echo "${gem} (>= ${version}) OK"
  else
    rc=$(($rc + 1))
  fi
done < gems/bundled_gems

exit $rc
