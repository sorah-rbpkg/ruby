=begin
= NAME

gem3.2 - frontend to RubyGems, the Ruby package manager

= SYNOPSIS

gem3.2 command [arguments...] [options...]

= DESCRIPTION

gem3.2 is the frontend to RubyGems, the standard package manager for Ruby.
This is a basic help message containing pointers to more information.

Further help:

: gem3.2 help commands
  list all gem3.2 commands

: gem3.2 help examples
  shows some examples of usage

: gem3.2 help ((|COMMAND|))
  show help on COMMAND, (e.g. 'gem3.2 help install')

= LINKS

http://rubygems.org/

= EXAMPLES

gem3.2 install rake
gem3.2 list --local
gem3.2 build package.gemspec
gem3.2 help install

= SEE ALSO

bundle(1)

=end
